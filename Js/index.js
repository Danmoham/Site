const toggleMenu = () =>{
    const myMenu = document.querySelector(".menu-links")
    const myIcon = document.querySelector(".hamburger-icon")
    myMenu.classList.toggle("open")
    myIcon.classList.toggle("open")

}